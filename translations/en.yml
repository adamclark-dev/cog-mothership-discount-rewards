ms.discount_reward:
  referral:
    constraints:
      minimum_order:
        name: Minimum order value
        description: Set the minimum order to trigger the reward creation
    triggers:
      order_create:
        name: Order creation
        description: The reward is created when the referred user fulfills an order
    types:
      discount_reward:
        name: Discount reward
        description: A reward where a discount is generated when a user is successfully invited to the site